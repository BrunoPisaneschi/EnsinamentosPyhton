"""
# No python, para criar uma variável é extremamente simples, basta escolher um nome e atribuir um valor utilizando o sinal de `=`

# Por conveção, em python, o nome das variáveis sao todas minúsculas, separadas por `_` underline.

# Variáveis globais, são todas maiúsculas, com o mesmo critério de separação de palavras

# Módulos (é o nome dos os arquivos .py) e diretórios seguem a mesma convenção do nome das variáveis.
"""


primeira_variável = "Parabéns, você criou uma variável!"


"""
Existem algumas palavras reservadas em python. 
Isso significa que você não deve usar certos nomes para criar variáveis, funções, classes, etc.

Algumas delas e suas definições:

  - pass -> Não tem nenhuma função, serve apenas para complementar o código, não gerando erro por indentação. Um exemplo de uso, é quando você cria uma classe ou função, mas ainda não vai desenvolver o conteúdo dela e quer apenas continuar o restante do código. Nesse caso, usa-se `pass` dentro da função, para ser ignorado a indentação.

  - continue -> Indica para um laço de repetição, que ele deve ir para o próximo índice da repetição.
  
  - break -> Indica para um laço de repetição, que ele deve ser interrompido.
  
  - id -> Função builtin (nativa) que retorna um ID único de um objeto.
  
  - def -> Representa que será criado uma função.
  
  - class -> Representa que será criado uma classe.
  
  - import -> Representa que alguma biblioteca será importada para o projeto.
"""
